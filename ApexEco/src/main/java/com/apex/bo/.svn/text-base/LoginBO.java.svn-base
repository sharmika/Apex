package com.apex.bo;

import com.apex.model.User;

/**
 * Interface has the following methods.
 * @author mahesh
 * @version 1.0
 */
public interface LoginBO {
	/*
	 * calling method to check the user authentication and getting user
	 * details.
	 */
	public User getUserDetails(Long userId) throws Exception;
	/*
	 * passing yearMonth value and returns longTerm chart related json
	 * String
	 */
	public String getChartDetails(String yearMonth);
	/*
	 * passing dayValue,megaWattValue value and returns Dayahead chart
	 * related json String
	 */
	public String getDayAheadChartDetails(String xValue, String yValue);

}
